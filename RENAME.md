# 建议把 GitHub 仓库改名为（一次性）

**Settings → General → Repository name：**

```text
pg-sam-zym-quotes-zh
```

改名后旧链接 `founder-blog-zh` 会自动跳转。本地可选：

```powershell
git remote set-url origin https://github.com/FranklinNexus/pg-sam-zym-quotes-zh.git
```
